#include <stdio.h>

int main(void)
{
    float x, y;
    int i, j;
    
    /* Exercise 4
    scanf("%d%f%d", &i, &x, &j);
    printf("%d-%f-%d\n", i, x, j);
    */

    // Exercise 5
    scanf("%f%d%f", &x, &i, &y);
    printf("%f|%d|%f\n", x, i, y);

    return 0;
}